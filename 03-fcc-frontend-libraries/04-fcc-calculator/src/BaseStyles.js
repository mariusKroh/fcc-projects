import { createUseStyles } from "react-jss";

const styles = createUseStyles({
  root: {
    width: "100vw",
    height: "100vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#f1a8d3",
  },
});

export default styles;
